e = [1,2,3,5,6,7,8]


Where to put 4?

(smallest number is always on top!)

int count;

count = 0;

int x = 4;

// we are working with linked lists
if (e)
{
    if (e->value < x)
        return (count);
    e->next;
    count++;
}